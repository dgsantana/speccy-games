[package]
name = "mm-core"
description = "Manic Miner game engine, independent of any window or audio backend"
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[features]
# Developer switches: cavern jumping, guardians, air and lives. Off by default;
# a build without it contains none of the code.
debug = ["speccy/debug"]

[dependencies]
mm-data.workspace = true
speccy.workspace = true

[dev-dependencies]
speccy.workspace = true

[lints]
workspace = true
